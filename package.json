{
  "name": "huxy-admin",
  "version": "1.4.0",
  "description": "Free admin dashboard template. Built with React18, Webpack5, useRouter, useStore, eslint, prettier, husky, commitlint, etc.",
  "keywords": [
    "React18",
    "useRouter",
    "useStore",
    "layout",
    "low-code",
    "no-code",
    "web-design",
    "scene-design",
    "web3",
    "webgl",
    "hooks",
    "Webpack5",
    "eslint",
    "prettier",
    "husky",
    "commitlint",
    "admin",
    "template",
    "dashboard"
  ],
  "sideEffects": [
    "*.css",
    "*.less"
  ],
  "scripts": {
    "postinstall": "husky install",
    "start": "pack start",
    "build": "pack run build",
    "analyze": "pack run analyze",
    "server": "pack run server",
    "test": "pack run test",
    "eslint": "eslint 'app/**/*.{js,jsx}'",
    "stylelint": "stylelint 'app/**/*.{css,less}'",
    "lint": "npm run eslint && npm run stylelint",
    "lint-fix": "eslint --fix 'app/**/*.{js,jsx}' && stylelint --fix 'app/**/*.{css,less}'",
    "prettier": "prettier 'app/**/*' --write --ignore-unknown && npm run lint-fix",
    "release": "pack run release"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/ahyiru/huxy-admin"
  },
  "license": "MIT",
  "devDependencies": {
    "@huxy/pack": "^0.2.4",
    "husky": "^8.0.3"
  },
  "dependencies": {
    "@huxy/components": "^1.9.1",
    "@huxy/layout": "^1.9.1",
    "@huxy/materials": "^0.2.1",
    "@huxy/router": "^1.9.1",
    "@huxy/use": "^1.9.1",
    "@huxy/utils": "^1.9.1",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}
